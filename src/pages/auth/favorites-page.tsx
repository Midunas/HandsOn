import React from 'react';
import { Box, Typography } from '@mui/material';
import ApplicationBar from '../global/components/application-bar/index';

const FavoritesPage = () => (
  <>
    <ApplicationBar />
    <Box>
      <Typography variant="h4" sx={{ mt: -30 }}>
        Nothing here... yet.
      </Typography>
    </Box>
  </>
);

export default FavoritesPage;
